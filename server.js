/**
 * StreamWave / YouTube Realtime Platform
 * Root Entry Point Proxy
 *
 * For hosting platforms (e.g. Render, Railway, Heroku) when Root Directory
 * is set to the repository root instead of the /backend directory.
 */
require('./backend/server.js');
